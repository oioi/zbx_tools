zbx_mail: needs to be rewritten to be more 'modular'. Cache must be moved to a separate class.
logger: if compiled with optimisation flags, in some certain situations call to fprintf inside logger causes segfault. O_O
